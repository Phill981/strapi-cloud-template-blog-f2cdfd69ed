'use strict';

/**
 * ambassador-requirement service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::ambassador-requirement.ambassador-requirement');

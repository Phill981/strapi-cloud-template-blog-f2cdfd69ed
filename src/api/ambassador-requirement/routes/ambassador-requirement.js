'use strict';

/**
 * ambassador-requirement router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::ambassador-requirement.ambassador-requirement');

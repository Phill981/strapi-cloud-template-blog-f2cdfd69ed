'use strict';

/**
 * engagement-area controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::engagement-area.engagement-area');

'use strict';

/**
 * ambassador-benefit service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::ambassador-benefit.ambassador-benefit');

'use strict';

/**
 * ambassador-benefit router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::ambassador-benefit.ambassador-benefit');
